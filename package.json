{
  "name": "montfort-api",
  "version": "1.0.0",
  "description": "Montfort - Agent Infrastructure API",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "dev": "nodemon api/server.js",
    "site": "npx serve site -p 3000",
    "setup": "npm install && cp .env.example .env"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "stripe": "^13.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
